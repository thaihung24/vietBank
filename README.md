# vietBank

`*` Ứng dụng là sản phẩm demo nên chỉ thực hiện các chức năng như <b>
`*` Đăng nhập, tạo đơn hàng , kiểm tra đơn hàng<b>
`*` Tài khoản demo<b>
email : 19110221@student.hcmute.edu.vn<b>
pass : 123456

`*`Dữ liệu trong ứng dụng được fake, tạo cứng nên không thể tạo thêm.<b>
`*`Ví dụ <b>
`*`Khi tiến hành tạo đơn hàng và đi thanh toán.<b>
`*`Người dùng sẽ được đưa đến trang thanh toán của momo và tiến hành thanh toán.<b>
`*`Sau khi thanh toán xong sẽ redirect về trang home.<b>
`*`Xem các đơn hàng ở nút giỏ hàng ở trang home.<b>
` *``* `Vì chưa tích hợp cơ sở dữ liệu<b>
Như đã được đề cập ở trên , vì là fake data nên đơn hàng người dùng vừa thanh toán sẽ không hiển thị ở đây.<b>
`*`Mà chỉ có các đơn đã được quản trị viên add từ trước để thực hiện tính năng kiểm tra đơn hàng.<b>
`*`Note : hãy đảm bảo bạn đã login vào hệ thống.<b>
`*`Một phiên làm việc có hiệu lực 15 phút.<b>

`*`Ứng dụng có tích hợp tailwind<b>
`*`sử dụng NPM i để tải các gói phụ thuộc<b>
`*`npm run build để build file css<b>
`*`Sản phẩm demo, vừa code vừa tìm hiểu công nghệ nên còn nhiều thiếu sót <b>
`*` Cảm ơn bạn đã đọc!<b>
